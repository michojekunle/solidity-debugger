
import { vi } from "vitest"

// Mock vscode API
// vi.mock("vscode") removed in favor of alias to src/test/vscode-mock.ts

// Mock ethers provider if needed (though usually mocked in specific tests)
